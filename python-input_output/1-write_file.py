def write_file(filename="", text=""):
    # Open the file using the correct mode
    with open(filename, mode="w") as numbers:
        # Write the text to the file
        text = numbers.write(________)

    # Return the number of characters written
    return ________
